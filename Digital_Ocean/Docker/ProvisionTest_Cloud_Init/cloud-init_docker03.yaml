#cloud-config
package_update: true
package_upgrade: true
write_files:
 - path: /root/setup.sh
   content: |
     #!/bin/bash
     curl -H "Authorization: token ${GITHUB_TOKEN}" -L https://raw.githubusercontent.com/NEEDGITGOOD/Terraform_IaC/main/Digital_Ocean/Docker/ProvisionTest_Cloud_Init/docker-compose_docker03.yaml -o /root/docker-compose.yaml
   permissions: '0700'
runcmd:
 - /root/setup.sh
 - docker compose -f /root/docker-compose.yaml up -d